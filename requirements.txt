# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# HTML parsing
beautifulsoup4==4.12.2

# HTTP requests
requests==2.31.0

# Environment variables
python-dotenv==1.0.0

# Data validation and settings management
pydantic==2.5.0

# Type hints support for older Python versions (if needed)
typing-extensions==4.8.0

# Optional: for better performance with BeautifulSoup
# lxml==4.9.3

# Optional: JSON handling (usually included in Python standard library)
# But good to have explicit version for consistency
# Note: json is part of Python standard library, no need to include

# Optional: Regular expressions (part of Python standard library)
# Note: re is part of Python standard library, no need to include

# Optional: Development and testing dependencies
# pytest==7.4.3
# pytest-asyncio==0.21.1
# black==23.11.0
# flake8==6.1.0